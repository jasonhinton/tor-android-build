name: Build libtor.so for Android

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source
      uses: actions/checkout@v3

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y ninja-build cmake curl unzip build-essential

    - name: Download NDK
      run: |
        mkdir -p $HOME/android
        cd $HOME/android
        curl -LO https://dl.google.com/android/repository/android-ndk-r25b-linux.zip
        unzip android-ndk-r25b-linux.zip
        mv android-ndk-r25b ndk

    - name: Configure and build Tor
      run: |
        export ANDROID_NDK_HOME=$HOME/android/ndk
        export PATH=$ANDROID_NDK_HOME:$PATH
        export TOOLCHAIN=$ANDROID_NDK_HOME/toolchains/llvm/prebuilt/linux-x86_64
        export CC=$TOOLCHAIN/bin/aarch64-linux-android21-clang
        export CXX=$TOOLCHAIN/bin/aarch64-linux-android21-clang++

        mkdir build-android
        cd build-android

        cmake .. \
          -G Ninja \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_TOOLCHAIN_FILE=$ANDROID_NDK_HOME/build/cmake/android.toolchain.cmake \
          -DANDROID_ABI=arm64-v8a \
          -DANDROID_PLATFORM=android-21

        ninja

    - name: Upload libtor.so
      uses: actions/upload-artifact@v3
      with:
        name: libtor-android
        path: build-android/src/app/libtor.so
